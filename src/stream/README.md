Stream code owns proxy-style byte streams where OpenSSH keeps control of the
visible connection: raw byte relays, proxy workers, stream liveness, status
output, and mux routing for proxy stream frames.
