Session code owns terminal-emulator sessions: visible-client presentation,
terminal-worker lifecycle, PTY handling, input translation, repaint state, and
the mux handler that connects daemon tunnel streams to a terminal worker.
